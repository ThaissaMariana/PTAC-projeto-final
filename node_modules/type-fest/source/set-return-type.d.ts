 or
all of your files are text files, then you can set the <code>merge</code> attribute
for those files to <code>lfs-text</code> and use <code>git config</code> to set the merge
driver like so:</p>
</div>
<div class="listingblock">
<div class="content">
<pre class="highlight"><code class="language-console" data-lang="console">$ git config merge.lfs-text.driver 'git lfs merge-driver --ancestor %O --current %A --other %B --marker-size %L --output %A'</code></pre>
</div>
</div>
<div class="paragraph">
<p>This tells Git to invoke the custom Git LFS merge driver, which in turn
uses Git&#8217;s merge machinery, to merge files where the <code>merge</code> attribute
is set to <code>lfs-text</code>. Note that <code>lfs-text</code> here is an example and any
syntactically valid value can be used.</p>
</div>
<div class="paragraph">
<p>If you are using a special type of file that needs rules different from
Git&#8217;s standard merge machinery, you can also specify the <code>--program</code>
option, which is passed to <code>sh</code> after substituting its own
percent-encoded escapes:</p>
</div>
<div class="ulist">
<ul>
<li>
<p><code>%A</code>: the current version</p>
</li>
<li>
<p><code>%B</code>: the other version</p>
</li>
<li>
<p><code>%D</code>: the destination version</p>
</li>
<li>
<p><code>%O</code>: the ancestor version</p>
</li>
<li>
<p><code>%L</code>: the conflict marker size</p>
</li>
</ul>
</div>
<div class="paragraph">
<p>Note that the percent sign must typically be doubled to prevent Git from
substituting its own values here. Therefore, specifying the default
behavior explicitly looks like this:</p>
</div>
<div class="listingblock">
<div class="cont